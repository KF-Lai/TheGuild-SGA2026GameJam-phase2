interface:
  display_name: "Map Systems"
  short_description: "Map game systems, dependencies, and design order."
  default_prompt: "Use $map-systems to decompose this concept into systems."

policy:
  allow_implicit_invocation: true
