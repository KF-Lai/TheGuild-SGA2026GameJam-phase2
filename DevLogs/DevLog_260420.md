# DevLog — 2026-04-21

## 概述

完成 Core 層全部 6 份 GDD（C-01 已於前次完成，本日補完 C-02、C-04、C-05、C-06），並對 F-01～F-03、C-01～C-06 執行三輪系統審查，處理邏輯一致性問題、架構設計決策與工程實作矛盾，合計修正 20+ 個問題點。

---

## 執行內容

### GDD 設計完成

- **C-02 Adventurer Management** — 冒險者實例、4 狀態機（Idle/Dispatched/Wounded/Dead）、fixedTraitIDs + randomTraitGroupIDs 分欄、慘勝倖存者 → Wounded
- **C-04 Race System** — 4 種種族、modifiers 以 JSON 儲存、ProfessionRacePool 加權隨機（C-04 自行實作）
- **C-05 Trait System** — 3 種 effectType（stat/behavior/condition）、完整 effectTarget 變數空間、5 個 TraitGroup、Fisher-Yates shuffle 自行實作
- **C-06 World Danger System** — 5 階危險度、時間閘 + 進度閘 + 陣營閘（三閘全表格化）、遞迴 CheckLevelUp

### GDD 命名規範套用

- **全 GDD 重命名** — 統一改為 `[ID] system-name.md` 格式（Haiku subagent 執行）

### 邏輯一致性審查（C-01～C-06）

- **Critical #1 — 破產門檻衝突** — F-03 改用 C-06 `GetMaxDebt()` 作為金幣硬性下限；`BankruptcyThresholdTable` 僅保留 `warningDurationSec`
- **Warning #2 — C-06 架構層次** — FT-09 → C-06 陣營閘改為觀察者模式（`OnFactionScoreUpdated` 推送）
- **Warning #8 — 慘勝倖存者** — 決議強制進入 Wounded 狀態
- **自動修正（7 項）** — pickMode 術語統一為 `"uniform"`、GetAllMissionTypes API 新增、stat 疊加規則補充、systems-index schema 同步等

### 工程邏輯審查（F-01～C-06，Claude Code 執行）

- **W-1** — F-01 C-04 下游表格移除錯誤的 `PickRandom` 引用
- **W-2** — F-01 C-06 下游表格將 `GetFloat(MissionPoolWeights)` 更正為 `GetAll<MissionPoolWeightData>`
- **W-3** — F-02 下游補入 C-02（OnSecondTick 驅動 Wounded 恢復）；C-02 TickWoundedRecovery 說明更正
- **W-4/W-5** — F-03 Edge Cases AddGold(0)/AddReputation(0) 廣播行為與 Formula 矛盾修正（不廣播）
- **W-6** — F-03 `OnOfflineResolved()` 簽名補全為 `(long offlineSeconds, int completedCount)`
- **M-1** — C-06 `gameStartTimestamp` 補充初始化機制說明（FT-10 寫入）
- **M-2** — C-05 `Shuffle` 補充 Fisher-Yates 實作說明

---

## 待解決問題

- **Codex shell 環境失效** — `CreateProcessWithLogonW failed: 2`，Codex MCP 無法在此 Windows 機器上執行 shell 命令，導致 F-01～C-06 工程審查無法由 Codex 執行，本次改由 Claude Code 直接審查。需排查原因（可能為 Windows 登入工作階段或沙箱權限問題）後，才能恢復 Codex 實作流程。

---

## 建議明日繼續

- **解決 Codex shell 問題** — 確認 `CreateProcessWithLogonW failed: 2` 的根因（Windows 服務帳號 / sandbox 配置），修復後才能啟動 F-01～F-03 Codex 實作
- **FT 層 GDD 設計** — 按優先序從 FT-02 Mission Dispatch 開始（成功率/死亡率公式為核心循環關鍵）
- **Haiku 量產特質資料** — 依 C-05 定義的 effectTarget 變數空間，產出 TraitTable.csv 初始內容
- **啟動 Codex 實作 F-01～F-03** — shell 問題解決後，Foundation 層三個系統已有完整 GDD，可進入實作階段
