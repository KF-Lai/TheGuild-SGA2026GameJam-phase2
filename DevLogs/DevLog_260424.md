# DevLog — 2026-04-24

## 概述

完成 F-01~F-03 實作（Codex 多輪審查後通過，尚待 Unity MCP 驗證），並針對 FT-08 Guild Staff System 完成設計方向對齊（Q1~Q6 + D1~D7 全部決策落地），同時確立全專案時間單位規則（僅秒/小時）。

---

## 執行內容

### 程式實作完成

- **F-01 DataManager / F-02 Time System / F-03 Resource Management** — 經 Claude Code 與 Codex 多輪審查後全數實作完成；尚未進 Unity MCP 驗證
- 新增目錄與檔案：
  - `TheGuild-unity/Assets/Scripts/Core/Data/`、`Core/Events/`、`Core/Time/`
  - `TheGuild-unity/Assets/Scripts/Gameplay/Resources/`
  - `TheGuild-unity/Assets/Resources/Data/Tables/`
  - `TheGuild-unity/Assets/Tests/`、`ProjectSettings/ScriptExecutionOrder.asset`

### FT-08 Guild Staff System 設計對齊

前次對話中斷後重新對齊使用者答案，完成以下關鍵決策：

- **職員結構** — 每個職員為獨立角色，職能改為 tag（CSV list），舊「3 種具名職員」API（`GetStaffWillingnessBonus` / `GetAccountantCommissionBonus` / `GetAccountantPenaltyBonus`）**名稱維持不變**，內部改為聚合 tag
- **招募（Gacha）** — 依公會等級刷新 50/100/300g、無限制刷、每 10 抽保底 5★（跨卡池不跨等級）、Jam 版單一常駐池；稀有度 1★40/2★25/3★15/4★15/5★5；每次刷新張數按等級遞增；垃圾物品兩類型（flavor item + 平庸職員）
- **薪水** — 每日 UTC 06:00 發薪（玩家電腦時區）、離線補發 N 次、休假不支薪、時區對不上則跳過不扣款；走 FT-05 既有 `OnStaffSalaryDue` 契約
- **職員狀態** — 三態互斥（工作中 / 再分配 / 休假）+ 冷卻正交；切換建築冷卻 2 小時；再分配 >12 小時（含離線）自動轉休假；解雇無冷卻但不可逆、需付資遣費
- **資料表** — `StaffTable` 新增 tags / rarity / severancePay / isFiller / salary；`slotBuildingID` 可多選（空位填 0）；`BuildingTable` 全 6 棟加 `slotCount`；新增 `StaffRefreshCostTable`、`StaffGachaPoolTable`、`StaffRarityProbTable`、`TrashItemTable`
- **名冊上限** — Jam 版固定 99（無人資辦公桌建築）

### 規範建立

- **時間單位全局規則** — 全專案時間參數只能用「秒」或「小時」；禁用分鐘/天/週；UI 呈現層例外；既有 GDD 若違反需排入反向依賴修正清單

### 記憶系統更新

- **`feedback_time_units.md`** — 時間單位全局規則
- **`project_ft08_design_decisions.md`** — FT-08 Q1~Q6 + D1~D7 完整決策清單（下次 `/design-system` 輸入依據）
- **`MEMORY.md`** — 新增兩則指針

---

## 建議明日繼續

- **FT-08 GDD 正式撰寫** — 用 `/design-system` 以 `project_ft08_design_decisions.md` 為輸入逐節產出 8 section；同時補「招募時薪水滿意度」概念進 `game-concept.md` 未來構想
- **F-01~F-03 Unity MCP 驗證** — 實作已完成但尚未驗證；建議跑 EditMode/PlayMode 測試與場景實測，確認 AC-DM / AC-TS / AC-EB / AC-RM 全數通過後再動 FT-08 以後系統
- **FT-08 通過後合併處理 FT-05 反向依賴清單** — 目前 `project_ft05_reverse_deps.md` 列的 9 項雙向依賴更新仍等 FT-05 整體通過後啟動；FT-08 GDD 通過時再合併一次處理，可減少一輪全 GDD 修訂
- **git commit 歸檔** — F-01~F-03 實作檔、Tests 目錄、ScriptExecutionOrder、work-items 變更都尚未 commit；建議收尾時按系統分 commit
